// =========================================================
//  SYSARCH — Seed (no-op: seeding is handled server-side)
// =========================================================

export function seedData(): void {
  // Data is seeded by the Express server at startup.
  // See server/index.js → seedDefaultData()
}
